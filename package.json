{
  "name": "bluebubbles-server",
  "version": "1.9.9",
  "description": "BlueBubbles Server is the app that powers the BlueBubbles app ecosystem",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "concurrently \"cd ./packages/ui && npm run start\" \"cd ./packages/server && npm run start\"",
    "build-ui": "cd ./packages/ui && npm run build && mkdir -p ../server/dist/static && rm -rf ../server/dist/static && cp -R ./build/** ../server/dist/ && cd ../../",
    "build-bbd": "npm run build -w @bluebubbles/bbd",
    "build-server": "cd ./packages/server && npm run dist && cd ../../",
    "release-server": "cd ./packages/server && npm run release && cd ../../",
    "build": "npm run build-ui && npm run build-bbd && npm run build-server && rm -rf ./dist && mkdir -p ./dist && cp -R ./packages/server/releases/* ./dist/ && rm -rf ./packages/server/releases/ && rm -rf ./packages/ui/build/",
    "release": "npm run build-ui && npm run build-bbd && npm run release-server",
    "prepare": "husky"
  },
  "repository": "https://github.com/munkledata/gator",
  "author": "munkledata <munkledata@gmail.com>",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/munkledata/gator/issues"
  },
  "homepage": "https://github.com/munkledata/gator",
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": ">=24.0.0"
    },
    "packageManager": {
      "name": "npm",
      "version": ">=10.0.0"
    }
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "husky": "^9.1.7"
  },
  "dependencies": {
    "typescript": "5.6.3"
  },
  "overrides": {
    "node-addon-api": "^8.0.0",
    "esbuild": "^0.28.0"
  }
}
